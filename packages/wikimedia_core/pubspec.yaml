name: wikimedia_core
description: "Core package for Wikimedia API and HTML processing."
version: 1.0.0
publish_to: 'none'

environment:
  sdk: ^3.11.4

dependencies:
  flutter:
    sdk: flutter
  beautiful_soup_dart: ^0.3.0
  html: ^0.15.6
  http: ^1.6.0
  shared_preferences: ^2.5.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
