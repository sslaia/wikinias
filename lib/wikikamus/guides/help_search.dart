import 'package:flutter/material.dart';

class HelpSearch extends StatelessWidget {
  const HelpSearch({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Help in search"));
  }
}
