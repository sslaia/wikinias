import 'package:flutter/material.dart';

class HelpWrite extends StatelessWidget {
  const HelpWrite({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Help in writing"));
  }
}
